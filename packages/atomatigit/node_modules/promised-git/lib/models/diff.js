(function() {
  var Diff;

  Diff = (function() {
    function Diff(path, raw) {
      var chunks, line;
      this.path = path;
      this.raw = raw != null ? raw : '';
      chunks = raw.split(/^@@(?=[ \-\+\,0-9]*@@)/gm);
      this.chunks = (function() {
        var _i, _len, _ref, _results;
        _ref = chunks.slice(1);
        _results = [];
        for (_i = 0, _len = _ref.length; _i < _len; _i++) {
          line = _ref[_i];
          _results.push('@@' + line);
        }
        return _results;
      })();
    }

    return Diff;

  })();

  module.exports = Diff;

}).call(this);
