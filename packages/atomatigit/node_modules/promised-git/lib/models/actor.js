(function() {
  var Actor;

  Actor = (function() {
    function Actor(raw) {
      var email, m, name, _ref, _ref1;
      if (/<.+>/.test(raw)) {
        _ref = /(.*) <(.+?)>/.exec(raw), m = _ref[0], name = _ref[1], email = _ref[2];
        _ref1 = [name, email], this.name = _ref1[0], this.email = _ref1[1];
      } else {
        this.name = raw;
      }
    }

    Actor.prototype.toString = function() {
      if (this.email) {
        return "" + this.name + " <" + this.email + ">";
      } else {
        return this.name;
      }
    };

    return Actor;

  })();

  module.exports = Actor;

}).call(this);
